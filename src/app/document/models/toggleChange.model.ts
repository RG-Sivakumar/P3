export class toggleValue {
  receiveValue: any;
}
export class filterToolbar {
  points: boolean;
  freehand: boolean;
  Vertex: boolean;
  Rectangle: boolean;
  all: boolean;

  //   public get all(): boolean {
  //     return this.points && this.freehand && this.Vertex && this.Rectangle;
  //   }

  //   public set all(v: boolean) {
  //     this._all = v;
  //   }
}
